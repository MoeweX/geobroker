# server information
[server]
brokerId = "broker"
port = 5559
granularity = 5
messageProcessors = 1

    # server mode specific information (only one should be uncommented)
#    [server.mode]
#    name = "single"

    [server.mode]
    name = "disgb_subscriberMatching"
    brokerAreaFilePath = "connect_and_disconnect.json"
    brokerCommunicators = 1

