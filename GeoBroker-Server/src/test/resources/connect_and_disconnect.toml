# server information
[server]
brokerId = "broker"
port = 5559
granularity = 5
messageProcessors = 1

    # server mode specific information (only one should be uncommented)
    [server.mode]
    name = "single"


